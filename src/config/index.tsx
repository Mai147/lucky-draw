import { ThemeName } from "../constants/Theme/Theme";

const AppConfig = {
    DEFAULT_THEME: ThemeName.TET,
    DEFAULT_MAX_VALUE: 169,
    DEFAULT_APP_HEADER: "MỪNG XUÂN BÍNH NGỌ"
}

export default AppConfig;